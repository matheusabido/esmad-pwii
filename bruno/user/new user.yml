info:
  name: new user
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/user
  body:
    type: json
    data: |-
      {
        "name": "teste",
        "password": "senha123",
        "email": "batata@batata.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
