info:
  name: PATCH user
  type: http
  seq: 5

http:
  method: PATCH
  url: http://localhost:3000/user/1
  body:
    type: json
    data: |-
      {
        "password": "admin123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
