info:
  name: POST status
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/status
  body:
    type: json
    data: |-
      {
        "name": "teste",
        "description": "teset123",
        "color": "aa33bb"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
