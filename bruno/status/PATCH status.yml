info:
  name: PATCH status
  type: http
  seq: 4

http:
  method: PATCH
  url: http://localhost:3000/status/1
  body:
    type: json
    data: |-
      {
        "description": "weeeeeee"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
