info:
  name: PATCH incident
  type: http
  seq: 5

http:
  method: PATCH
  url: http://localhost:3000/incident/6
  body:
    type: json
    data: |-
      {
        "priority": "high"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
