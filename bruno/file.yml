info:
  name: file
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/file
  body:
    type: multipart-form
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
