info:
  name: GET categories
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/categories
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
