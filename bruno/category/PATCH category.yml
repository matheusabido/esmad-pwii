info:
  name: PATCH category
  type: http
  seq: 4

http:
  method: PATCH
  url: http://localhost:3000/category/1
  body:
    type: json
    data: |-
      {
        "description": "weeeeeee"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
