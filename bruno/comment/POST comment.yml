info:
  name: POST comment
  type: http
  seq: 3

http:
  method: POST
  url: http://localhost:3000/comment
  body:
    type: json
    data: |-
      {
        "incidentId": 7,
        "comment": "teset123123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
