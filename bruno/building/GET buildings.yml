info:
  name: GET buildings
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/buildings
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
