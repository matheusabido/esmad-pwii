opencollection: 1.0.0

info:
  name: pwii
config:
  proxy:
    inherit: true
    config:
      protocol: http
      hostname: ""
      port: ""
      auth:
        username: ""
        password: ""
      bypassProxy: ""

request:
  auth:
    type: bearer
    token: "{{at}}"
bundled: false
extensions:
  bruno:
    ignore:
      - node_modules
      - .git
